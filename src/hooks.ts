import type { Transport } from '@sveltejs/kit';
// import { ObjectId } from 'mongodb';

export const transport: Transport = {
	// ObjectId: {
	// 	encode: (value: ObjectId) => value.toHexString(),
	// 	decode: ([id]: string) => new ObjectId(id)
	// }
};
